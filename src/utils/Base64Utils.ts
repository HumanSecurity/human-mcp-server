import { IBase64Utils } from 'perimeterx-js-core';

// TODO: consider changing this class with js-core "Base64Utils"
export class Base64Utils implements IBase64Utils {
    base64Decode(stringToDecode: string): string {
        // TODO implement
        throw new Error('not implemented');
    }

    base64Encode(stringToEncode: string): string {
        // TODO implement
        throw new Error('not implemented');
    }
}
